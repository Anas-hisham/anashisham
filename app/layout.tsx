import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Anas Hisham | Full Stack Developer",
  description: "Versatile Full Stack Developer with expertise in web and mobile technologies. Skilled in React, Next.js, React Native, TypeScript, and Node.js.",
  keywords: ["Full Stack Developer", "React", "Next.js", "React Native", "TypeScript", "Node.js", "MongoDB", "Portfolio"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
